
요지 : 주요정보통신기반시설을 기반으로 취약점 진단 ·조치까지 자동화를 진행하는 스크립트 자동화

Shell script + Ansible + Web Dashboard

> CSAP 가이드 내 기술을 가져와서 취약점 진단하면 조금은 도움이 될지도


---

프로젝트 정의

주요기반시설 기술적 취약점 분석·평가 상세 가이드를 기준으로 취약점 진단 및 조치 스크립트를 개발하고, Ansibile과 중앙 웹 대시보드를 결합해 수십~수백 대 서버를 자동으로 점검·조치 보고하는 플랫폼을 구축한다.

대상 환경 : Rokcy Linux 9, 10 / Ubuntu 24, 26 / Window server / DB / Web server


Ansible 내 inventory에  서버 위치를 저장시켜놓고 뿌려놓으면 됨

기본적으로 취약점 진단은 
- shell script로 개별 진행
- 취약점 진단은 ansible로 가능할지도 

결과 파일 추천
- json, csv


---

실무 현장의 4가지 문제

1. 수동 진단의 고비용 구조
서버마다 접속해 스크립트 실행. 결과를 눈으로 확인. 보고서는 수작업. 서버가 수백~수천대면 시간 인력 소모가 막대하여 휴먼 에러가 발생
2. 진단 결과의 비일관성
점검자마다 판단 기준, 숙련도가 달라 같은 서버도 결과가 다름 KISA라는 국가 표준을 코드로 고정하면 누가 돌려도 같은 결과
3. 컴플라이언스 수요 증가
ISMS-P 인증 및 주요기반시설 정기 점검 등 제도적 요구는 매년 증가하고 , 일회성 점검이 아닌 `반복 가능한 상시 점검 체계` 필요
4. 실제 보안 사고
SKT USIM 정보 유출, KT BGP 설정 오류 장애, 현대차그룹 임직원 개인정보 유출
서버 DB 관리 미흡이 대형 사고로 이어진 실제 사례들

---

AS-IS -> TO-BE
작업 방식 : 서버별 개별 접속, 순차 수동 점검 => Ansible 기반 다중 서버 병렬 자동 점검
운영 구조: 개별 서버 단위의 분산 관리 => Conrtol node 기반 중앙 집중 관리
판단 기준: 점검자 개인 경험,판단에 의존 => KISA 가이드 기준을 코드로 표준화
결과 관리: 비정형 텍스트 로그. 수기 취합 => JSON 표준 포맷으로 DB 저장으로 이력 관리 용이
가시성: CLI 중심, 전체 현황 파악 지연=> 웹 대시보드 실시간시각화(점수,분포)
보고서: 수작업 엑셀 작성(수 시간~ 수일) => 클릭 한번으로 Execl/PDF 자동 생성
조치: 일회성 수동 조치, 이력 없음 => 선택적 자동 조치 + 재진단 + 전/후 비교
유지보수: OS별 파편화된 스크립트 => 모듈화된 스크립트 라이브러리(재사용 자산)


---

구현 범위 - 5가지 코어 기능
1. 진단 · 조치 스크립트
	1. KISA U-01-67
	2. check/fix 쌍
	3. OS 2종 이상
	4. JSon 표준 출력
2. Ansible 다중 서버 자동화
	1. Inventory 기반
	2. SSH 병렬 실행
	3. OS 자동 식별
	4. 결과 중앙 수집
3. 중앙 웹 대시 보드
	1. 서버 등록 관리
	2. 진단 실행 · 시각화
	3. 이력(History)
	4. 보안 점수 차트
4. 안전한 조치 자동화
	1. 자동/수동 분류
		1. 취약 판단 기준에 대한 설명 기재 필요.
	2. 조치 전 백업
	3. 조치 후 재진단
	4. Before/After 비교


---
- KISA 가이드 UNIX 항목 U01-67까지
- JSON 스키마의 출력 포맷이 동일하게 확인할 수 있도록 한다.
	- check_id, category, status, current_value, expected_value,hostname,os_type,timestamp


---

Ansible 자동화 및 중앙 웹 대시보드
- Ansobie 다중 서버 자동화
- Control Node 1대에서 Inventory 등록 서버 전체에 SSH 병렬 실행
- OS 자동 식별(ansible_os_family)후 알맞은 스크립트 자동 선택
	- 스크립트 배포 -> 실행 -> JSON 결과 회수까지 Playbook으로 자동화
	- 진단 결과는 대상 서버에 남기지 않고 중앙에서만 관리(정보 노출 방치)
- 데모는 VM 4~6대 , 설계는 수백 대 확장을 전제로


중앙 웹 대시보드
- 서버 등록/ 삭제 - 개별 입력 + CSV 일괄 등록
- 전체/개별 서버 진단 실행 버튼과 진행 상태 표시
- 시각화 - 보안 점수, 양호/취약/수동확인 분포, 서버 별 항목별 상세
- 이력(History) 관리 - 단발 점검이 아닌 '개선 추세'가 보여야 함
- 취약 항목 조치 실행 -> 자동 재진단 -> Before/After 비교

---

조치 자동화
- 설정 파일 수정 등 영향 범위 예측이 가능한 항목
- 조치 전 우너본 설정 백업 필수
- 기준을 맞추는 '최소한의 변경'만 수행
- 조치 후 자동 재진단으로 최종 상태 검증


---
보고서 자동 생성

기본 요구사항
- 진단/ 조치 결과를 Excel 보고서로 클릭 한번에 출력
- 요약부 - 보안 점수, 전체 통계, 양호/취약 분포 차트
- 상세부 - 서버별 항목별 결과, 판단 근거, 조치 전/후 내용
- 취약 항목 위험도 기준 정렬 + 색상 강조 ?

---

제출 산출물
최종 보고서 PPT+PDF - 개요/ 팀 구성 역할/ 일정(WBS)/ 아키텍처/ 주요 기능/ 시연 / 자체 평가, 한계, 개선 방향
시연 영상 - 서버 등록 -> 진단-> 결과 확인-> 조치 -> 재진단-> 보고서 출력
소스코드 -> ZIP + Github 진단. 조치 스크립트/Ansible/대시보드/ README(구조 · 실행 방법)


---

프로젝트 참고사항

1. JSON 스키마 확정
2. 자동 조치는 위험을 알계 설게
3. 판단 근거를 남겨라
4. 예외 처리에 강해야한다-파일 없음, 권한 부족,연결 실패
5. 취약 테스트 os 로 검증 - 의도적 취약/양호로 정탐 ,오탐 검증
6. 결과는 중앙에만
7. 수백대는 설계 전제로 말해라